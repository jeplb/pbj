name: ci

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  build-and-test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: install zlib (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: sudo apt-get update && sudo apt-get install -y zlib1g-dev

      - name: build (release)
        run: make

      - name: run fixture tests
        run: make test

      - name: build (debug, sanitizers)
        run: |
          make clean
          make debug

      - name: run fixture tests under sanitizers
        run: make test

  static-linux:
    runs-on: ubuntu-latest
    container: alpine:3.20
    steps:
      - name: install build deps
        run: apk add --no-cache build-base musl-dev zlib-dev zlib-static make
      - uses: actions/checkout@v4
      - name: static build
        run: make CFLAGS="-O3 -Wall -Wextra -std=c11 -static -pthread"
      - name: smoke test
        run: ./pbj --version
      - name: verify static linking
        run: file pbj && (ldd pbj 2>&1 || true)
      - uses: actions/upload-artifact@v4
        with:
          name: pbj-linux-x86_64-static
          path: pbj
